#pragma once

namespace systems::leal::campello_widgets
{

    /**
     * @brief Brightness mode for a design system.
     */
    enum class Brightness
    {
        light, /**< Light color scheme. */
        dark,  /**< Dark color scheme. */
    };

} // namespace systems::leal::campello_widgets
