#include <campello_widgets/widgets/animated_size.hpp>

// All logic is inline in the header and in render_animated_size.cpp.
// This translation unit exists so CMake includes the header in the build graph.
